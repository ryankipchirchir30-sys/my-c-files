#include<stdio.h>
int main() {
printf("ryan is a good guy\n");
int age = 19;// this stores an interger variable  in %d format
char grade ='C';//this stores a single line character in %c format
float gpa = 5.7; // this stores a float point number which is simply a decimal number stored in %f format
char university[] = "kibabii"; // this stored a  string variable which include multiple characters in %s format


printf("he is %d years old\n", age);// this print the variable age
printf("he scored an average of '%c'\n",grade);//this prints the character variable grade
printf("the school he attends is %s\n",university);//this prints the stringvariable university  
printf("his average gpa is %f\n",gpa);// thhis prints the float variable gpa



return 0;
}